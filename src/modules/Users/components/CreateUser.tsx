import React from "react";

const CreateUser: React.FC = React.memo(() => {
  return (
    <React.Fragment>
      <p>CreateUser under construction 🛠️</p>
    </React.Fragment>
  );
});

export default CreateUser;
